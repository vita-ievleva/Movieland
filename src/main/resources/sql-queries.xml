<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <entry key="movies.select.allMovies">
        SELECT
        movie.id,
        name,
        year,
        rate,
        genre FROM movie
        INNER JOIN movie_genres
        ON movie.id=movie_genres.id_movie
        ORDER BY [conditions];
    </entry>
     <entry key="default.order.by">
         rate desc, price asc
     </entry>

    <entry key="movie.select.byId">
        SELECT
         id
        ,name
        ,year
        ,country
        ,description
        ,rate FROM movie
        WHERE movie.id = :id;
    </entry>

    <entry key="genre.select.byMovieId">
        SELECT id_movie, genre FROM movie
        INNER JOIN movie_genres
        ON movie.id=movie_genres.id_movie
        WHERE movie.id = :id;
    </entry>

    <entry key="review.select.byMovieId">
        SELECT id_movie, review_text FROM movie
        INNER JOIN review
        ON movie.id=review.id_movie
        WHERE movie.id = :id;
    </entry>

    <entry key="movies.select.bySearchParameters">
        SELECT movie.id, name, year, rate, genre FROM movie
        INNER JOIN movie_genres
        ON movie.id=movie_genres.id_movie
        WHERE [conditions];
    </entry>

    <entry key="users.byUserName">
        SELECT users.username, password, authority, enabled
        FROM users INNER JOIN authorities
        ON authorities.username = users.username where users.username = :username;
    </entry>

    <entry key="users.byUserName">
        SELECT users.username, password, authority, enabled
        FROM users INNER JOIN authorities
        ON authorities.username = users.username where users.username = :username;
    </entry>

</properties>